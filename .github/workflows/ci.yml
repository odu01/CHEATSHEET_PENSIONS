name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Catches the failure mode the DYNAMIC_PYRAMID_WEB repo shipped with: a
      # published site whose files do not resolve. Every dataset, column, view,
      # page, script src and import path is checked against what is on disk.
      - name: Validate manifest and asset references
        run: node tools/validate-manifest.mjs

      # The files in data/vstup/ are filled in by hand, so the contract they are
      # supposed to match has to be checked against what is actually in them:
      # header, types, empty cells, duplicate keys, gaps in a run of years.
      - name: Validate hand-filled input files
        run: node tools/check-vstup.mjs

      # The palette in lib/theme.js is derived (brand hues snapped to passing
      # OKLCH steps), so it has to be re-measured rather than trusted.
      - name: Validate palette accessibility gates
        run: node tools/check-palette.mjs

      # The CSVs in data/ are derived from the workbooks in data/zdroj/. Re-running
      # the import must reproduce them byte for byte; a diff means someone edited
      # a generated file by hand and the next import would silently overwrite it.
      - name: Check data/ matches the source workbooks
        run: |
          pip install --quiet openpyxl
          python3 tools/import_sp.py
          if ! git diff --quiet -- data/; then
            echo "::error::CSV v data/ nesúhlasia s zošitmi v data/zdroj/."
            echo "Spusti 'npm run data' a commitni výsledok. Needituj generované CSV ručne."
            git diff --stat -- data/
            exit 1
          fi

      # Synthetic input files must be reproducible from their generator, the same
      # way data/ must be reproducible from the workbooks. The check only covers
      # files still flagged illustrative in the manifest — once real numbers
      # arrive and the flag goes, the generator stops owning that file.
      - name: Check synthetic input files match their generator
        run: python3 tools/gen_vstup.py --check

      - name: Syntax-check every module
        run: |
          for f in app.js lib/*.js tools/*.mjs tools/validate_palette.js; do
            node --check "$f" || exit 1
          done
          echo "Všetky moduly sa parsujú."

  smoke:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # No lockfile is committed (there are no runtime deps — the libraries are
      # vendored), so this is `npm install`, not `npm ci`.
      - name: Install Playwright (Chromium only)
        run: npm install --no-audit --no-fund && npx playwright install --with-deps chromium

      # Renders every page in both modes and fails on any console error, any
      # unhandled rejection, any failed request, and any card that rendered an
      # error box. A chart that throws at runtime is not something the static
      # checks above can see.
      - name: Render every page and fail on any console error
        run: node tools/screenshots.mjs --check

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: screenshots/
          retention-days: 14
          if-no-files-found: warn
