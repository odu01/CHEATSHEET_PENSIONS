{
  "name": "cheatsheet-pensions",
  "version": "0.1.0",
  "private": true,
  "description": "Dátovo riadené grafy a tabuľky k dôchodkovému (PAYGO) systému. Statický web bez build stepu.",
  "type": "module",
  "scripts": {
    "serve": "node tools/serve.mjs",
    "validate": "node tools/validate-manifest.mjs && node tools/check-vstup.mjs && node tools/check-palette.mjs",
    "validate:manifest": "node tools/validate-manifest.mjs",
    "validate:palette": "node tools/check-palette.mjs",
    "vstup": "node tools/check-vstup.mjs",
    "vstup:gen": "python3 tools/gen_vstup.py",
    "data": "python3 tools/import_sp.py",
    "shots": "node tools/screenshots.mjs"
  },
  "comment": "There are no runtime dependencies on purpose: d3, Observable Plot and the Plotly gl3d bundle are vendored under vendor/ at pinned versions. devDependencies are only needed for `npm run shots`.",
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "engines": {
    "node": ">=18"
  }
}
